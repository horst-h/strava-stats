// not in use just for refactoring purposes
class AthleteStats {
    constructor(name, age, sport, target, stats) {
        this.name = name;
        this.age = age;
        this.sport = sport;
        this.target = target;
        this.stats = stats; // stats can be an object containing various statistics
    }
}